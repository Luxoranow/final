const fs = require('fs'); const path = require('path'); const { createCanvas } = require('canvas'); const canvas = createCanvas(320, 180); const ctx = canvas.getContext('2d'); ctx.fillStyle = 'blue'; ctx.fillRect(0, 0, 320, 180); ctx.fillStyle = 'white'; ctx.font = '24px Arial'; ctx.textAlign = 'center'; ctx.fillText('Test', 160, 90); const buffer = canvas.toBuffer(); fs.mkdirSync(path.join('public', 'backgrounds', 'thumbnails'), { recursive: true }); fs.writeFileSync(path.join('public', 'backgrounds', 'thumbnails', 'test.jpg'), buffer); console.log('Test image created');
