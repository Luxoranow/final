const fs = require('fs'); const path = require('path'); const { createCanvas } = require('canvas'); const canvas = createCanvas(1920, 1080); const ctx = canvas.getContext('2d'); ctx.fillStyle = 'blue'; ctx.fillRect(0, 0, 1920, 1080); ctx.fillStyle = 'white'; ctx.font = '120px Arial'; ctx.textAlign = 'center'; ctx.fillText('Test Background', 960, 540); const buffer = canvas.toBuffer(); fs.mkdirSync(path.join('public', 'backgrounds'), { recursive: true }); fs.writeFileSync(path.join('public', 'backgrounds', 'test.jpg'), buffer); console.log('Full-size test image created');
